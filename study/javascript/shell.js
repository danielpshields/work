function getStartingWidth() {
  return window.innerWidth;
}

function getStartingHeight() {
  return window.innerHeight;
}

function newTab() {
  return window.open("url");
}

// alert(getStartingWidth(), "innerWidth");
// alert(getStartingHeight(), "outerHeight");
newTab();
